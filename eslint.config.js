import { defineConfig } from "@rainbowatcher/eslint-config";

export default defineConfig({
    gitignore: true,
    typescript: true,
    style: true,
    json: true,
    markdown: true,
    yaml: true,
});